const dependable=require('dependable');
const path=require('path');

const myModule=[['_','lodash']];
myModule.forEach(function(val){
    container.register(fnction(){
         return (val[1])
    })
   
});
